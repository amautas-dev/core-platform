/** Rutas del catálogo unificado (productos, planes, módulos, features). */
export const CATALOG_PREFIX = '/catalog';
export const CATALOG_PRODUCTS = `${CATALOG_PREFIX}/products`;
export const CATALOG_SERVICE_PLANS = `${CATALOG_PREFIX}/service-plans`;
export const CATALOG_MODULES = `${CATALOG_PREFIX}/modules`;
export const CATALOG_FEATURES = `${CATALOG_PREFIX}/features`;
